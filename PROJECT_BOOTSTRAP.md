# PROJECT_BOOTSTRAP.md — AI 接管入口（唯一）

> 这是整个项目**唯一的 AI 接管入口（Single Entry Point）**。
> 任何新的 AI（WorkBuddy / Claude / GPT / Gemini / Cursor / Codex 等）在开始开发前，**必须首先阅读本文件**，并**禁止依赖聊天记录**作为项目记忆。
> 项目的唯一可信来源是：**GitHub 最新代码 + 根目录 6 份文档 + `docs/` 目录**（见第 1 节）。

---

## 1. 文件作用

- 这是整个项目**唯一的 AI 接管入口**。
- 任何新的 AI 在开始开发之前，**必须首先阅读本文件**。
- **禁止**依赖聊天历史作为项目记忆。
- 项目的唯一可信来源应为：
  - **GitHub 最新代码**（main 分支，已部署 GitHub Pages）
  - `README.md`
  - `PROJECT.md`
  - `VISION.md`
  - `TODO.md`
  - `CHANGELOG.md`
  - `AI_RULES.md`
  - `docs/` 目录（含 `Prompt/` 等）

---

## 2. AI 接管流程

**Step 1 — 阅读文档**
依次阅读：`README.md` → `PROJECT.md` → `VISION.md` → `AI_RULES.md` → `TODO.md` → `CHANGELOG.md` → 整个 `docs/` 文件夹。

**Step 2 — 扫描代码**
扫描整个项目代码，理解：
- **页面**：6 个底部标签（今日 / 学习 / 财富 / 身体 / 自媒体 / 我的）
- **模块**：`U` / `S` / `Charts` / `AI` / `EnglishMod` / `Pages` / `App`
- **架构**：单文件 `index.html`、零依赖、localStorage 持久化（内存回退）
- **数据结构**：`pgwb_data_v1` / `pgwb_settings_v1`
- **Prompt**：`docs/Prompt/`
- **PWA**：`manifest.json` / `sw.js`（离线缓存 + 版本更新）
- **GitHub Pages**：main 根目录静态托管，HTTPS，push 自动部署

**Step 3 — 总结**
向用户输出总结，覆盖：
- 产品定位
- 当前版本
- 已实现功能
- 未完成功能
- 技术架构
- 待办事项

**Step 4 — 等待需求**
- 等待用户提出需求。
- **禁止**：直接修改代码。
- **禁止**：自行优化。
- **禁止**：增加功能。

---

## 3. 开发原则

所有开发遵循以下顺序：

```
需求分析
   ↓
方案确认
   ↓
修改代码
   ↓
自检
   ↓
更新文档
   ↓
Commit
   ↓
Push
   ↓
等待用户测试
```

---

## 4. 文档维护原则

每完成一个功能，**必须**同步更新：
- `PROJECT.md`
- `TODO.md`
- `CHANGELOG.md`
- 以及 `docs/` 中所有受影响文档

保证文档始终与代码一致。

---

## 5. Git 工作流

所有修改：
- **Commit**
- **Push**
- 必要时 **Tag**（`v1.0` / `v1.1` / `v2.0` …；开发态 `version.json` 为 `dev`）

Commit Message 使用前缀：
- `feat:` 新功能
- `fix:` 修复 Bug
- `docs:` 仅文档
- `refactor:` 重构（不改外部行为）
- `perf:` 性能优化
- `style:` 仅格式（**非配色 / 非 UI 视觉**）

---

## 6. AI 行为规范

AI **不允许**：
- 擅自修改 UI
- 擅自修改颜色
- 擅自修改动画
- 擅自修改数据结构
- 擅自删除功能
- 擅自修改 `localStorage`
- 擅自修改部署方式

如果发现更好的方案：
- **必须先说明理由**
- **等待用户确认**
- 不得直接修改

---

## 7. 输出规范

每次开发完成后，输出：
- **修改文件**：本次改动了哪些文件
- **修改原因**：为什么改
- **影响范围**：可能影响哪些功能 / 模块 / 部署
- **需要测试哪些功能**：建议用户验证的点

---

## 8. 新聊天规范

如果是一个**全新的聊天**：

1. 第一件事：**阅读 `PROJECT_BOOTSTRAP.md`**。
2. 然后按照本文件规定的流程完成项目接管（Step 1–4）。
3. 完成接管后，**必须**向用户输出：
   1. 当前项目版本
   2. 当前产品定位
   3. 当前模块
   4. 当前技术架构
   5. 当前待开发内容
4. 最后回复：
   > 项目已完成接管，等待新的开发需求。
5. **不得直接开始修改代码。**

---

## 9. 长期维护原则

`PROJECT_BOOTSTRAP.md` 是整个项目**唯一的 AI 接管入口**。

以后如果：
- 增加新文档
- 修改开发规范
- 修改 Git 工作流
- 修改 Prompt 管理方式
- 修改文档结构

**必须同步更新 `PROJECT_BOOTSTRAP.md`**，保证任何新的 AI 都可以仅通过阅读本文件完成项目接管。
